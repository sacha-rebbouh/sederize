# Supabase Configuration
# Get these values from your Supabase project settings (Settings > API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Supabase Service Role Key (for server-side operations like backups)
# Get this from your Supabase project settings (Settings > API > service_role)
# WARNING: Never expose this key client-side!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Cron Secret (for securing automated backup endpoint)
# Generate a random string: openssl rand -hex 32
CRON_SECRET=your-random-secret-here

# PowerSync Configuration (for offline-first sync)
# Get this from your PowerSync dashboard (https://powersync.com)
# Format: https://your-instance.powersync.journeyapps.com
NEXT_PUBLIC_POWERSYNC_URL=
